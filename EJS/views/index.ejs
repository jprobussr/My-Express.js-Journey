<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Weekday Warrior</title>
    <style>
      body {
        background-color: black;
        color: aliceblue;
      }
    </style>
  </head>
  <body>
    <h1><%= title %></h1>

    <p>Current Second: <%= seconds %></p>

    <% if (seconds % 2 === 0) { %>
    <ul>
      <% items.forEach((fruit) => { %>

      <li><%= fruit %></li>
      <% }) %>
    </ul>
    <% } else {%>
    <p>No items to display</p>
    <% }%>

    <p><%- htmlContent %></p>

    <%- include("footer.ejs") %>

    <!-- <ul>
        <%# for(let i = 0; i < fruits.length; i++) { %>
            <li>
                <%#= fruits[i] %>
            </li>
            <%# } %>
    </ul> -->
    <!-- <h1>Hi, <%#= name %></h1>
    <h2>Hey, it's <%#= dayType %>, <%#= advice %>!</h2> -->
  </body>
</html>
